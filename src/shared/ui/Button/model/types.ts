export type SubmitButtonStatuses = 'success' | 'failed' | null

/*
 * timed — animation disappears after a delay
 * static — animation stays visible permanently
 */
export type SubmitButtonModes = 'timed' | 'static'
